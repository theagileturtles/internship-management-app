import {
    createConnection,
    query
  } from "../data_access/database";
  
//TODO: use auth middleware
export default function handler(req, res) {
    //Post -> Get completed internship application based on req.user.id
}
  